<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>Blog</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
	<style>
		body {
			padding-top: 20px;
			padding-bottom: 20px;
		}

		.jumbotron {
			margin-top: 10px;
		}
	</style>
</head>

<body>
	<div class="container">
		<%- include('partials/navbar') %>
		<div class="jumbotron">
			<h1>Pocetna strana</h1>
			<p class="lead">Vesti iz Sirbistana i sveta!</p>
		</div>

		<div class="row">
			<div class="col-lg-12">
				<div class="list-group">
				<!-- loop over blog posts and render them -->
					<% posts.forEach((post) => { %>
					<a href="/post/<%= post.id %>" <% if(post.author === 'Vucic') { %> class="list-group-item list-group-item-danger"> <span class="badge">(FOTO) (VIDEO) (TROLOL)</span> <% } else { %> class="list-group-item"> <% } %>
						
						<h4 class="list-group-item-heading"><%= post.title %></h4>
						<p class="list-group-item-text">Autor: <%= post.author %></p>
					</a>
					<% }); %>
				</div>
			</div>
		</div>
		<%- include('partials/footer') %>
	</div>
</body>

</html>